import { CurrencyInput2Directive } from './currency-input2.directive';

describe('CurrencyInput2Directive', () => {
  it('should create an instance', () => {
    const directive = new CurrencyInput2Directive();
    expect(directive).toBeTruthy();
  });
});
