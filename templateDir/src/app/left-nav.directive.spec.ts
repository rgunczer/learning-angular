import { LeftNavDirective } from './left-nav.directive';

describe('LeftNavDirective', () => {
  it('should create an instance', () => {
    const directive = new LeftNavDirective();
    expect(directive).toBeTruthy();
  });
});
